# Device-specific environment config.
# Copy this file to ~/.env and fill in the values for this machine:
#   cp ~/.env.example ~/.env
# ~/.env is not tracked by the dotfiles repo (it holds per-device paths/secrets).

# Secrets
export SYNCTHING_API_KEY=""

# Paths
export OBSIDIAN_VAULT=""
